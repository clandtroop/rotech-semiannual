{
  "name": "rotech-semiannual-root",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npm --prefix rotech-semiannual run dev",
    "build": "npm --prefix rotech-semiannual install && npm --prefix rotech-semiannual run build:hosted && node -e \"const fs=require('fs');fs.rmSync('dist',{recursive:true,force:true});fs.cpSync('rotech-semiannual/dist','dist',{recursive:true})\"",
    "deploy": "npm --prefix rotech-semiannual run deploy"
  }
}
